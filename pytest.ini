[pytest]
# Judge-facing: `pytest -q` collects only the hermetic suite under tests/.
# scratch/ holds throwaway exploration that depends on private fixtures and is
# excluded from the open repo.
testpaths = tests
norecursedirs = scratch vendor .venv adk_app planning
